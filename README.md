# TDEI-api-client
Auto generated api client for the TDEI public APIs

This repository hosts the autogenerated client code for the swagger APIs hosted for TDEI gateway system.

TODO:
- Add the swagger API link
- Add steps on how to update this client library when the actual swagger is updated
- Add steps on the github actions requirement for the same.

Github action requirements:
- Create a webhook that listens to changes in the version of the APIs (option 1)
- Generate a manual github action that does the following
    - Generates the new code from the swagger (updated)
    - replaces the source code
    - Generates a PR with the updated code

NOTE: The above can be made via any other bash script as well. Need to figure out how.